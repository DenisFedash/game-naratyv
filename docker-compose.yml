version: "3"

services:
    game-naratyv:
      build: . 
      restart: always
      ports:
        - "5001:5001"
      volumes: 
      - /app/node_modules
      - .:/app
    server:
      build: ./server 
      restart: always    
      ports: 
        - "5556:5003"
      volumes: 
      - /app/node_modules
      - ./server:/app
